[project]
name = "physics-notes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jalsop24", email = "53493046+jalsop24@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.1.0",
    "ipython>=9.8.0",
    "sympy>=1.14.0",
]

[project.scripts]
physics-notes = "physics_notes:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "nbstripout>=0.8.2",
]
